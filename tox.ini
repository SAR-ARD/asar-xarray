[tox]
isolated_build = True
envlist = py312
requires = tox-conda
;skipsdist = True

[testenv]
deps =
    pytest
    pytest-cov
conda_deps =
    gdal
commands =
    coverage run -m pytest
    coverage xml

[coverage:run]
relative_files = True
source = asar_xarray/
branch = True