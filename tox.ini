[tox]
envlist = py312
;skipsdist = True

[tox:conda]
conda_deps =
    gdal

[testenv]
deps =
    pytest
    pytest-cov
commands =
    coverage run -m pytest
    coverage xml

[coverage:run]
relative_files = True
source = asar_xarray/
branch = True