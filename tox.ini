[tox]
envlist = py312
;skipsdist = True

[testenv]
deps =
    pytest
    pytest-cov
    gdal
commands =
    coverage run -m pytest
    coverage xml

[coverage:run]
relative_files = True
source = asar_xarray/
branch = True